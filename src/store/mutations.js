import {
  // RECORD_MENU_DATA
} from './mutation-types.js'

import { setStore, getStore } from '../config/mUtils'
//需要？

export default {
    // [INIT_FORM_ITEM_INDEX] (state,value) {
    //     state.formItemIndex = value
    // }
}
